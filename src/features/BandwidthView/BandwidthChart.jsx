const BandwidthChart = ({ chartRef }) => {

  // return <ReactECharts option={option} style={{ height: 400 }} />;
  return (
    <div className="chart-container" ref={chartRef} style={{ width: '100%', height: 400 }}>

    </div>
  )
}

export default BandwidthChart